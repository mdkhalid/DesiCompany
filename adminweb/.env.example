# API base URL (defaults to /api/v1 via Vite proxy in dev)
VITE_API_BASE=/api/v1

# Optional admin phone hint shown on the login screen. Leave empty (recommended)
# so no real number is bundled into the deployed frontend. Set only in trusted env files.
VITE_ADMIN_PHONE_HINT=
